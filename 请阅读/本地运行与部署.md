# 本地运行与部署交接

本文对应 2026-09-13 交付的源码。适用于从解压包或新 GitHub 仓库开始建立一个独立开发环境，不要对已在使用的数据库重复执行建表文件。

## 技术组成

- React 19 + TypeScript + Vinext，生成 Cloudflare Worker 后台与浏览器资源。
- Cloudflare D1 / SQLite：业务数据、会话、积分流水、管理员日志。
- Cloudflare R2：私有图片、视频及上传分段。
- Drizzle：数据库结构与迁移文件。
- pnpm：依赖和锁文件管理；Node.js 最低版本以 `package.json` 为准，目前为 22.13.0。

这不是只包含前端的项目。运行后台需要 `DB` 与 `FILES` 两个绑定，单纯执行静态文件预览无法提供注册、登录、发帖和积分功能。

## 一、安装依赖

在包含 `package.json` 的项目根目录执行：

```sh
pnpm install --frozen-lockfile
```

原有锁文件随源码交付。开发环境整理时使用 pnpm 11.19.0；`pnpm-workspace.yaml` 已列出 esbuild、sharp、workerd 的构建许可。

## 二、填写本地配置

复制 `.env.example` 为 `.env` 和 `.dev.vars`，在这两个文件中填写一致的值：

```dotenv
ADMIN_SETUP_TOKEN=替换成你新生成的随机凭证
ADMIN_EMAIL=替换成首个管理员邮箱
```

可以使用下面的命令生成随机凭证，将输出值填入两个文件。它是首次建立管理员的凭证，不是管理员日常登录密码：

```sh
node -e "process.stdout.write(require('node:crypto').randomBytes(32).toString('hex'))"
```

真实 `.env` 和 `.dev.vars` 已由 `.gitignore` 排除。交付包只含填写模板，不含原环境凭证。

## 三、初始化一个全新的本地数据库

以下四条命令按顺序运行一次。`wrangler.local.json` 中的数据库 ID 是本地占位值，不能作为正式云端数据库 ID 使用。

```sh
pnpm exec wrangler d1 execute DB --local --config wrangler.local.json --file drizzle/0000_watery_blockbuster.sql
pnpm exec wrangler d1 execute DB --local --config wrangler.local.json --file drizzle/0001_business_guards.sql
pnpm exec wrangler d1 execute DB --local --config wrangler.local.json --file drizzle/0002_milky_bug.sql
pnpm exec wrangler d1 execute DB --local --config wrangler.local.json --file drizzle/0003_media_guards.sql
```

全部成功后再启动网站。本地数据写入项目下的 `.wrangler` 目录，重启程序后会保留；删除这个目录会丢失该本地环境的数据。

## 四、启动并建立管理员

```sh
pnpm dev
```

打开终端显示的地址，通常为 `http://localhost:3000`。如果其他程序已占用 3000 端口，先停止那个程序，或按开发服务提示使用其他端口。

首次使用时打开 `/setup` 页面，填写：

- 自己选择的管理员用户名。
- 与 `ADMIN_EMAIL` 一致的邮箱。
- 自己选择的密码及确认密码。
- `ADMIN_SETUP_TOKEN` 中填写的初始化凭证。

管理员成功建立后，初始化入口会禁止再次建立首个管理员。管理员可以进入 `/admin`；普通成员从答题页面开始注册。

本地数据库与正式数据库是独立的，在一个环境建立管理员不会自动在另一个环境生成管理员。

## 五、验证与生产构建

```sh
pnpm exec tsc --noEmit
pnpm build
```

构建结果生成在 `dist/`，该目录不需要提交到 GitHub。`pnpm start` 用 Wrangler 在本地运行构建结果，不会自动发布到互联网上。

完整业务验收脚本需要 Python 3.10 或以上及 Pillow。它们会创建测试账号、帖子、积分和封禁记录，应使用单独的测试副本和全新的本地数据库，不要在你打算日常使用的环境运行。

先按前面的步骤初始化该测试副本，但让测试脚本建立 `AdminQA`，不要先手动创建另一个管理员。测试配置中的邮箱可以使用 `admin@example.invalid`。脚本假定使用原始默认准入题与默认规则，并连接 `http://localhost:3000`。

安装图片测试依赖，并准备示例视频：

```sh
python3 -m pip install Pillow
mkdir -p tests/.data
curl --fail --location https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4 --output tests/.data/sample.mp4
curl --fail --location https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.webm --output tests/.data/sample.webm
```

这些样本为 MDN 的 CC0 视频，交付包不含媒体二进制文件。MOV 测试使用兼容的 ISO BMFF/H.264 样本，不代表支持所有 MOV 编码。

保持本地开发服务运行，在另一个终端按顺序执行：

```sh
python3 tests/acceptance.py
python3 tests/security.py
python3 tests/media.py
```

安全与媒体脚本依赖第一个脚本建立的 `AdminQA` 测试账号。测试密码只是测试夹具，不是正式管理员凭证。缺少示例 MP4 时，核心脚本会跳过部分视频检查，因此不能声称复现了完整 107 项；`media.py` 需要两个样本都存在。

测试结果写入已忽略的 `tests/.data/`。连续五天签到使用历史测试日期模拟，详情见 `ACCEPTANCE.md`。

## 六、正式部署前的交接信息

上传 GitHub 后代码可供保存、协作和部署平台读取，但 GitHub 仓库本身不提供此应用所需的运行中后台与数据库。

当前 `.openai/hosting.json` 保留原 Sites 项目标识以及 `DB`、`FILES` 绑定名称；项目标识不是访问密钥。它供原站点配置使用，不能代替其他托管账号下的实际资源配置。`vite.config.ts` 使用这个文件，因此不要因它是隐藏目录就漏传。

沿用当前技术方案部署时，需要：

1. 可运行该 Worker 的托管环境。
2. 实际 D1 数据库与私有 R2 存储，并将它们绑定为 `DB`、`FILES`。
3. 在托管平台填写独立的正式 `ADMIN_EMAIL` 和 `ADMIN_SETUP_TOKEN`。
4. 正确执行数据库迁移，完成 Worker 发布。
5. 在正式网址建立管理员，验证完整业务流程和访问范围。

上次原 Sites 发布失败信息为 `incomplete input: SQLITE_ERROR`。本地迁移成功，但云端没有提供具体失败语句及已执行的迁移记录，因此错误根因尚未确认。本交付包保留原始迁移，没有删除历史、重新建站或清空云端数据库。

若修复原部署，应先取得云端迁移执行记录，确认具体失败位置后处理。若在你自己的全新环境部署，可以在新数据库中测试原始迁移；这不等于原云端问题已经修复。

若改用 Supabase，则需要把 D1/SQLite 的查询、事务和数据库规则改为兼容方案，并重新验证积分并发、删除回滚、封禁及锁定媒体权限。不能仅把 Supabase 地址填入现有配置就上线。

## 七、源代码与数据的范围

本包包含全部 106 个原始受版本管理文件。除了附加交付文档与校验清单外，原始文件保持字节一致，详情见 `SOURCE_MANIFEST.json`。

不包含本地真实数据、上传媒体、安装依赖缓存和旧 Git 历史。需要迁移实际用户数据时，应另外安排数据库和文件备份；源码包不作为数据备份。

未附加开源许可证，是否公开授权使用由代码所有者自行决定。
