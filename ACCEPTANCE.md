# 验收记录

验收日期：2026-09-05。运行环境：真实本地 Cloudflare D1/R2 开发服务，通过 HTTP 调用完整业务 API。

## 自动化结果

- 核心业务：77 项通过。
- 并发及安全扩展：26 项通过。
- 媒体容器与伪造文件反例：4 项通过。
- 合计：107 项通过。

核心场景 A–L 均覆盖，包括游客隔离、错误答题、通过答题注册、零积分新人、签到达到 10 分、发文奖励、达到 30 分评论、短评/长评、真实视频发帖、管理员邮箱查看、即时封禁、解封恢复。

待解锁场景覆盖低分过滤、高分解锁且不扣分、降分后重新锁定、低分作者豁免、管理员豁免、原图 GET/HEAD/Range 直接访问拒绝、全文锁定发布/编辑拒绝。额外覆盖视频封面复用锁定图的攻击反例。

并发检查覆盖：首次签到只有一次成功、重复签到不加分、并发评论奖励总额最多 6 分、并发水区发帖最多两帖获得积分。

上传检查覆盖 JPEG/PNG/GIF/WEBP，真实 H.264 MP4 与 WEBM、兼容的 ISO BMFF/H.264 MOV 上传路径、超过 8 MiB 的真实 PNG 分段上传后字节一致性、超限与未完成上传拒绝、伪造 MP4/WEBM 文件头拒绝。未用 500 MB 视频做完整压力测试；大小上限与跨分段传输分别验收。

## 页面交互

通过浏览器验证管理员登录、首页/搜索结果、后台用户表及邮箱展示；检查 1440 px 桌面与 390 px 手机布局和手机导航。页面内 WebMCP 的签到与搜索两项工具均完成正确输入、错误输入及页面状态回读验证。

## 交付检查

- TypeScript 类型检查通过。
- 生产构建通过，输出 Worker 默认 `fetch` 处理器。
- 数据库迁移生成并在本地成功应用。
- 本地测试数据与生产数据分离。
- 线上部署状态由 Sites 发布结果确认。

## 验收解释

连续五天签到使用历史测试日期模拟，不声称实际等待了五天。短评和长评完整单条奖励在独立账号验收；同日 +2 后的长评按每日 6 分上限只奖励剩余 4 分。视频没有服务端转码，编码兼容性限制详见 README。
